# Python Requirements for Blindsight V1 Integration
# Install with: pip3 install -r requirements_blindsight.txt

# Core dependencies
numpy>=1.19.0
scipy>=1.5.0
matplotlib>=3.3.0

# Computer vision
opencv-python>=4.5.0

# NEST Simulator Python interface
# Note: NEST itself must be installed separately (see SETUP_BLINDSIGHT.md)

# Optional: Hardware interfaces
pyserial>=3.5  # For Arduino communication
# picamera2  # For Raspberry Pi Camera (install via apt on RPi)

# Optional: Performance monitoring
psutil>=5.8.0

